{
  "name": "hp-pre-build",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i input.css -o ../docs/vendor.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "tailwindcss": "^4",
    "daisyui": "^5"
  }
}
